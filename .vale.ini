# The config the README gives, with the package's style read from the tree
# instead of a sync.
StylesPath = .
MinAlertLevel = suggestion

[{fixtures,fixtures/clean}/page.md]
BasedOnStyles = Readability
